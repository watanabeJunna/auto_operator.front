version: "3"

services:
  app:
    build: "./app"
    command: "npm run dev"
    volumes:
      - ".:/app"
    ports: 
      - "3000:3000"
    environment:
      - NODE_ENV=development
  api:
    build:
      context: "./api"
      dockerfile: "dev.Dockerfile"
    command: "sh ./up.sh"
    volumes:
      - "./api:/go/src/user"
    ports: 
      - "8081:8081"
    environment:
      - AES_KEY=${AES_KEY}
  db:
    image: "mongo:3.4.22-xenial"
    volumes:
      - "db:/data/db"
      - "configdb:/data/configdb"
    ports:
      - "27017:27017"
volumes:
  db:
  configdb: